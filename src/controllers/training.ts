import { RequestHandler } from 'express';
import { Training } from '../models/training';

const TRAININGS: Training[] = [];

export const getTrainings: RequestHandler = (req, res, next) => {

};

export const getTraining: RequestHandler = (req, res, next) => {

};

export const createTraining: RequestHandler = (req, res, next) => {

};

export const updateTraining: RequestHandler<{ id: string }> = (req, res, next) => {

};

export const deleteTraining: RequestHandler = (req, res, next) => {

};
